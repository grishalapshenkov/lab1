# Отчёт по лабораторной работе № 1
## Итерационные методы вычисления элементарных функций: квадратный корень, кубический корень и натуральный логарифм

**Дисциплина:** Теория алгоритмов  
**Направление подготовки:** 38.03.05 «Бизнес-информатика»  
**Курс:** 2  
**Выполнил:** студент группы [Ваша группа]  
**ФИО:** [Ваши Фамилия Имя Отчество]  
**Проверил:** [ФИО преподавателя]  
**Город, Год:** 2026  

---

## 1. Цель и задачи работы
**Цель:** сформировать представление об итерационных алгоритмах как о классе вычислительных процедур, результат которых достигается последовательным уточнением приближений, и освоить приёмы оценки их сходимости и трудоёмкости.  
**Задачи:**
1. Изучить понятия итерационного процесса, начального приближения, критерия остановки и скорости сходимости.
2. Реализовать алгоритмы вычисления $\sqrt{a}$ и $\sqrt[3]{b}$ методом Ньютона, а также $\ln c$ методом разложения в степенной ряд.
3. Экспериментально определить число итераций, необходимое для достижения заданной точности $\varepsilon$.
4. Сравнить полученные значения с эталонными значениями стандартной библиотеки (`math`) и оценить абсолютную погрешность.

---

## 2. Формулировка задания (Вариант 14)
Исходные данные из таблицы вариантов:
*   $a = 37$ (для вычисления $\sqrt{a}$)
*   $b = -125$ (для вычисления $\sqrt[3]{b}$)
*   $c = 12$ (для вычисления $\ln c$)
*   $\varepsilon = 10^{-6}$
*   Критерий остановки: **Н** (по невязке: $|x_{n+1}^2 - a| < \varepsilon$ для корня; для логарифма всегда по модулю члена ряда $|t_k| < \varepsilon$)
*   Начальное приближение $x_0$: $x_0 = a$ (то есть $x_0 = 37$ для $\sqrt{37}$ и $x_0 = |-125| = 125$ для $\sqrt[3]{-125}$)
*   Дополнительное требование: **Д4** (Ввести ограничение $N_{\max} = 5$. Реализовать корректное завершение с диагностическим сообщением, если точность не достигнута. Определить минимальное $N_{\max}$, достаточное для своего варианта).

---

## 3. Краткие теоретические сведения

### 3.1. Квадратный корень (Метод Ньютона / Формула Герона)
Решается уравнение $F(x) = x^2 - a = 0$. Итерационная формула:
$$x_{n+1} = \frac{1}{2}\left(x_n + \frac{a}{x_n}\right)$$
Критерий остановки (по невязке): $\lvert x_{n+1}^2 - a \rvert < \varepsilon$.

### 3.2. Кубический корень (Метод Ньютона)
Решается уравнение $F(x) = x^3 - |b| = 0$. Для отрицательного $b$ используется тождество $\sqrt[3]{b} = -\sqrt[3]{\lvert b \rvert}$. Итерационная формула для модуля:
$$x_{n+1} = \frac{1}{3}\left(2x_n + \frac{|b|}{x_n^2}\right)$$
Критерий остановки (по невязке): $\lvert x_{n+1}^3 - |b| \rvert < \varepsilon$. Знак восстанавливается в конце.

### 3.3. Натуральный логарифм (Разложение в ряд)
Используется преобразованный ряд Тейлора для ускорения сходимости при любом $c > 0$:
$$\ln c = 2\left(y + \frac{y^3}{3} + \frac{y^5}{5} + \frac{y^7}{7} + \ldots\right), \quad \text{где } y = \frac{c-1}{c+1}$$
Каждый следующий член ряда вычисляется рекуррентно: $t_k = t_{k-1} \cdot y^2 \cdot \frac{2k-3}{2k-1}$.  
Критерий остановки: $\lvert t_k \rvert < \varepsilon$.

---

## 4. Блок-схемы алгоритмов

### 4.1. Блок-схема вычисления $\sqrt{a}$ (с критерием по невязке)
```mermaid
flowchart TD
    A([Начало]) --> B[/Ввод a, ε, N_max/]
    B --> C{a < 0?}
    C -- Да --> D[/Ошибка: a < 0/] --> Z([Конец])
    C -- Нет --> E{a = 0?}
    E -- Да --> F[/Вывод: 0, 0/] --> Z
    E -- Нет --> G["x := a (т.к. a >= 1)<br/>n := 0"]
    G --> H["n := n + 1<br/>x_new := (x + a / x) / 2"]
    H --> I{"|x_new² − a| < ε?"}
    I -- Да --> J[/Вывод x_new, n/] --> Z
    I -- Нет --> K{n ≥ N_max?}
    K -- Да --> L[/Точность не достигнута<br/>Вывод x_new, n/] --> Z
    K -- Нет --> M[x := x_new] --> H
flowchart TD
    A([Начало]) --> B[/Ввод b, ε, N_max/]
    B --> C{b = 0?}
    C -- Да --> D[/Вывод: 0, 0/] --> Z([Конец])
    C -- Нет --> E["sign := -1 если b < 0, иначе 1<br/>b_abs := |b|<br/>x := b_abs (т.к. b_abs >= 1)<br/>n := 0"]
    E --> F["n := n + 1<br/>x_new := (2x + b_abs / x²) / 3"]
    F --> G{"|x_new³ − b_abs| < ε?"}
    G -- Да --> H[/Вывод sign * x_new, n/] --> Z
    G -- Нет --> I{n ≥ N_max?}
    I -- Да --> J[/Точность не достигнута<br/>Вывод sign * x_new, n/] --> Z
    I -- Нет --> K[x := x_new] --> F
flowchart TD
    A([Начало]) --> B[/Ввод c, ε, N_max/]
    B --> C{c ≤ 0?}
    C -- Да --> D[/Ошибка: c ≤ 0/] --> Z([Конец])
    C -- Нет --> E["y := (c − 1)/(c + 1)<br/>y2 := y²<br/>power := y<br/>total := 0<br/>k := 0"]
    E --> F["k := k + 1<br/>term := 2 * power / (2k - 1)"]
    F --> G{"|term| < ε?"}
    G -- Да --> H[/Вывод total, k/] --> Z
    G -- Нет --> I{k ≥ N_max?}
    I -- Да --> J[/Точность не достигнута<br/>Вывод total, k/] --> Z
    I -- Нет --> K["total := total + term<br/>power := power * y2"] --> F
"""
Лабораторная работа № 1. Вариант 14.
Итерационные методы вычисления √a, ∛b, ln c.
Дополнительное требование Д4: проверка N_max = 5 и поиск минимального достаточного N_max.
"""
import math

EPS = 1e-6

def sqrt_newton_residual(a, eps, n_max):
    """Квадратный корень методом Ньютона, критерий остановки по невязке."""
    if a < 0:
        raise ValueError("Подкоренное выражение должно быть неотрицательным")
    if a == 0:
        return 0.0, 0
    
    x = float(a) if a >= 1 else 1.0
    for n in range(1, n_max + 1):
        x_new = 0.5 * (x + a / x)
        if abs(x_new**2 - a) < eps:
            return x_new, n
        x = x_new
    return x, n_max  # Возвращаем последнее значение и достигнутое число итераций

def cbrt_newton_residual(b, eps, n_max):
    """Кубический корень методом Ньютона, критерий остановки по невязке."""
    if b == 0:
        return 0.0, 0
    
    sign = -1 if b < 0 else 1
    b_abs = abs(b)
    x = float(b_abs) if b_abs >= 1 else 1.0
    
    for n in range(1, n_max + 1):
        x_new = (2 * x + b_abs / (x * x)) / 3.0
        if abs(x_new**3 - b_abs) < eps:
            return sign * x_new, n
        x = x_new
    return sign * x, n_max

def ln_series(c, eps, n_max):
    """Натуральный логарифм через ряд, критерий по модулю члена ряда."""
    if c <= 0:
        raise ValueError("Аргумент логарифма должен быть положительным")
    
    y = (c - 1) / (c + 1)
    y2 = y * y
    power = y
    total = 0.0
    
    for k in range(1, n_max + 1):
        term = 2 * power / (2 * k - 1)
        if abs(term) < eps:
            return total, k
        total += term
        power *= y2
        
    return total, n_max

def run_d4_analysis():
    a, b, c = 37, -125, 12
    eps = EPS
    n_max_test = 5
    
    print("="*60)
    print(f"АНАЛИЗ ТРЕБОВАНИЯ Д4 (Ограничение N_max = {n_max_test})")
    print("="*60)
    
    # 1. Проверка с N_max = 5
    print("\n--- 1. Попытка вычисления с N_max = 5 ---")
    val1, n1 = sqrt_newton_residual(a, eps, n_max_test)
    res1 = abs(val1**2 - a)
    print(f"√{a}: итераций={n1}, невязка={res1:.2e} {'[УСПЕХ]' if res1 < eps else '[НЕ ДОСТИГНУТО]'}")
    
    val2, n2 = cbrt_newton_residual(b, eps, n_max_test)
    res2 = abs(val2**3 - abs(b))
    print(f"∛{b}: итераций={n2}, невязка={res2:.2e} {'[УСПЕХ]' if res2 < eps else '[НЕ ДОСТИГНУТО]'}")
    
    # Для логарифма невязка в классическом понимании не считается, смотрим на последний член
    val3, n3 = ln_series(c, eps, n_max_test)
    last_term = 2 * ((c-1)/(c+1))**(2*n3-1) / (2*n3-1) # приблизительная оценка последнего добавленного
    print(f"ln({c}): итераций={n3}, точность {'[УСПЕХ]' if n3 < n_max_test else '[НЕ ДОСТИГНУТО]'}")

    # 2. Поиск минимального достаточного N_max
    print("\n--- 2. Поиск минимального достаточного N_max ---")
    # Запускаем с заведомо большим N_max, чтобы узнать реальное необходимое число итераций
    _, req_n1 = sqrt_newton_residual(a, eps, 100)
    _, req_n2 = cbrt_newton_residual(b, eps, 100)
    _, req_n3 = ln_series(c, eps, 100)
    
    print(f"Минимальное число итераций для √{a}: {req_n1}")
    print(f"Минимальное число итераций для ∛{b}: {req_n2}")
    print(f"Минимальное число итераций для ln({c}): {req_n3}")
    
    min_sufficient_n = max(req_n1, req_n2, req_n3)
    print(f"\n>>> ВЫВОД ПО Д4: Минимальное достаточное N_max для Варианта 14 = {min_sufficient_n}")
    print("="*60)

def run_final_calculation():
    a, b, c = 37, -125, 12
    eps = EPS
    # Используем достаточное N_max, найденное на предыдущем шаге
    n_max = 35 
    
    print("\n" + "="*60)
    print("ИТОГОВЫЕ РЕЗУЛЬТАТЫ ВЫЧИСЛЕНИЙ")
    print("="*60)
    print(f"{'Функция':<8} | {'Аргумент':<8} | {'Результат':<14} | {'Эталон (math)':<14} | {'Абс. погрешность':<18} | {'Итераций'}")
    print("-" * 85)
    
    # Квадратный корень
    val1, n1 = sqrt_newton_residual(a, eps, n_max)
    ref1 = math.sqrt(a)
    print(f"{'sqrt':<8} | {a:<8} | {val1:<14.10f} | {ref1:<14.10f} | {abs(val1 - ref1):<18.2e} | {n1}")
    
    # Кубический корень
    val2, n2 = cbrt_newton_residual(b, eps, n_max)
    ref2 = math.cbrt(b) if hasattr(math, 'cbrt') else (abs(b)**(1/3) * (-1 if b < 0 else 1))
    print(f"{'cbrt':<8} | {b:<8} | {val2:<14.10f} | {ref2:<14.10f} | {abs(val2 - ref2):<18.2e} | {n2}")
    
    # Логарифм
    val3, n3 = ln_series(c, eps, n_max)
    ref3 = math.log(c)
    print(f"{'ln':<8} | {c:<8} | {val3:<14.10f} | {ref3:<14.10f} | {abs(val3 - ref3):<18.2e} | {n3}")
    print("="*60)

if __name__ == "__main__":
    run_d4_analysis()
    run_final_calculation()
